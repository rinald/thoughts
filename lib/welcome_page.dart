import 'package:flutter/material.dart';

// This page is displayed the first time the app gets opened.
// It promts the user to enter their username, which can be
// later changed in settings.
class WelcomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}